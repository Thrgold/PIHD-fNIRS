# Compatible lower bounds, not a recovered lockfile from the original experiment.
numpy>=1.21
scipy>=1.7
matplotlib>=3.5
torch>=1.12
scikit-learn>=1.0
PyWavelets>=1.1
# Required for the actual EMD baseline; do not rely on its SSA fallback.
EMD-signal>=1.2
